-- PostgreSQL Migration Script Template
-- Use this file for incremental schema changes against an existing database.

BEGIN;

-- Example migration statements:
-- ALTER TABLE teams ADD COLUMN archived_at TIMESTAMPTZ;
-- UPDATE teams SET archived_at = NOW() WHERE inactive = TRUE;

COMMIT;